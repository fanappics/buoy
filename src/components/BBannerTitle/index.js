import BBannerTitle from './BBannerTitle.vue'

/* istanbul ignore next */
BBannerTitle.install = function install (Vue) {
  Vue.component(BBannerTitle.name, BBannerTitle)
}

export default BBannerTitle
